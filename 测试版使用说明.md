# 测试版使用说明

## 最简单的测试方法

1. 双击 `启动工具.bat`。
2. 首次测试可直接使用已经预填的 `sample_projects/示例办公楼.xlsx`。
3. 选择与建筑所在地对应的 EPW，确认 OpenStudio 和 EnergyPlus 路径显示正确。
4. 点击“开始一键计算”。
5. 进度达到 100% 后，界面会显示基准用电、涂层用电和变化率。
6. 点击“打开最近结果”查看全部模型、日志和 CSV/JSON 文件。

## 测试自己的建筑

1. 复制 `templates/建筑参数输入模板.xlsx`，不要直接修改空白母版。
2. 按设计图填写项目、楼层、热区多边形、门窗、材料和构造。
3. 按实际运行填写人员设备、时间表、温控和 HVAC。
4. 在“涂料”工作表填写实测太阳反射率、长波发射率及目标构造。
5. 在界面选择填写后的 Excel 和建筑所在地 EPW，开始计算。

## 在其他设备上使用

1. 从 GitHub 克隆或下载项目；
2. 安装 Python 3.10+、OpenStudio 3.11.0 和 EnergyPlus 26.1.0；
3. 双击 `启动工具.bat`；
4. 首次运行时选择本机的软件和气象文件路径；
5. 本机路径保存在 `settings.json`，该文件不会上传到 GitHub。

## 应优先查看的结果

- `results/comparison_results.csv`：年度汇总和节电率；
- `results/hourly_cooling_comparison.csv`：8760 小时制冷用电对比；
- `generated_model/geometry_validation.json`：几何验收；
- `coating_model/coating_validation.json`：涂层模型差异验收；
- `results/stage8_validation.json`：结果完整性验收；
- `workflow_manifest.json`：本次运行的输入、输出和阶段状态。

## 结果解释

制冷节电率计算公式为：

`(基准制冷用电 - 涂层制冷用电) / 基准制冷用电 × 100%`

正值表示节电，负值表示用电增加。供暖变化率单独列出，避免只看夏季收益而
忽略冬季供暖代价。

## 当前测试版边界

- 涂层按热阻和热容可忽略的薄膜处理；
- 当前只允许目标表面 100% 覆盖；
- 支持 `Material` 和 `Material:NoMass` 外层材料；
- 不支持绿化屋面、复杂光谱大气窗口耦合和部分表面自动拆分；
- 示例数据只用于软件测试，不能作为真实工程结论。
